<%= link_to restaurant.name, restaurant_path(restaurant) %>
<%= restaurant.address %>
<%= restaurant.category %>
<% restaurant.reviews.each do |review| %>
  <%= review.content %>
  <%= review.rating %>
<% end %>
